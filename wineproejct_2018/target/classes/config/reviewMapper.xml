<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review.dao.ReviewMapper">

	<select id="reviewList" parameterType="int" resultType="Review">
		select * from review where userId = #{userId} order by reviewId desc limit #{startRow}, #{size}
	</select>
	<select id="reviewSize" parameterType="int">
		select count(*) from review where userId = #{userId} 
	</select>

	<delete id="reviewDelete" parameterType="int">
		delete from review where userId = #{userId}
	</delete>

	<insert id="reviewWrite" parameterType="Review">
		insert into review(productId, userId, loginId,
		productName, content)
		values(#{productId},#{userId},#{loginId},#{productName},#{content})
	</insert>
</mapper>